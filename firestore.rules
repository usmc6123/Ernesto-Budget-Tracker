rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Default-deny catch-all to prevent direct client SDK querying or manipulation
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
